$kind: http-request
description: Retrieves the authenticated patient's profile.
url: "{{baseUrl}}/api/patients/profile"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
